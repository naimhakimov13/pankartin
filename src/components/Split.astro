---
// Two-column layout. Pages place content in the `left` and `right` named slots.
// On ≤980px the columns collapse to one (handled in global.css).
---
<div class="split">
  <slot name="left" />
  <slot name="right" />
</div>
